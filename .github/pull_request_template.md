## What

<!-- One or two sentences describing the behavior change or fix from the user's perspective. -->

## Checklist

- [ ] `gofmt` has been run on changed Go files
- [ ] `go vet ./...`, `go test ./...`, and `go build ./...` are green
- [ ] User-visible behavior is documented in `README.md` (or the change is invisible to users)
